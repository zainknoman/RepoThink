
import {hybridSearch,selectContext} from "../intelligence/search.js";
export function retrieveEvidence(index,question,options={}){const results=hybridSearch(index,question,{limit:options.limit||30});return {question,results,context:selectContext(index,results,options),evidence:results.map((r,i)=>({id:"E"+(i+1),path:r.path,line:r.line||1,score:r.score,reason:r.evidence}))}}
export function buildEvidencePrompt(bundle){return "You are RepoThink. Answer only from supplied repository evidence. If insufficient, say so. Cite evidence IDs such as [E1].\n\nQUESTION:\n"+bundle.question+"\n\nEVIDENCE:\n"+bundle.evidence.map(e=>"["+e.id+"] "+e.path+":"+e.line+" score="+e.score.toFixed(2)).join("\n")+"\n\nCONTEXT FILES:\n"+bundle.context.files.join("\n")}
export async function askAI(config,bundle){if(!config?.apiKey)throw new Error("Configure an API key first.");const endpoints={openai:"https://api.openai.com/v1/chat/completions",local:"/v1/chat/completions",anthropic:"https://api.anthropic.com/v1/messages",gemini:"https://generativelanguage.googleapis.com/v1beta/openai/chat/completions"};const res=await fetch(config.endpoint||endpoints[config.provider]||endpoints.local,{method:"POST",headers:{"Content-Type":"application/json",Authorization:"Bearer "+config.apiKey},body:JSON.stringify({model:config.model||"gpt-4o-mini",messages:[{role:"user",content:buildEvidencePrompt(bundle)}]})});if(!res.ok)throw new Error("AI request failed: "+res.status);const data=await res.json();return data.choices?.[0]?.message?.content||data.content?.[0]?.text||JSON.stringify(data)}
