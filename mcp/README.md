# RepoThink MCP Server

Run: `node mcp/server.mjs C:\\path\\to\\repository`

The server implements MCP-style initialize, tools/list and tools/call JSON-RPC methods for RepoThink repository intelligence. It reads only the supplied local repository and has no runtime dependencies.
